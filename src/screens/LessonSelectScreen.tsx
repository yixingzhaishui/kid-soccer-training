import { scenePacks } from '../lessons';
import type { Progress,SceneCategory,ScenePack } from '../types/soccer';
type PackProps={progress:Progress;onHome:()=>void;onPack:(id:SceneCategory)=>void};
export function PackSelectScreen({progress,onHome,onPack}:PackProps){return <main className="screen select-screen"><header className="screen-header"><button className="icon-button" onClick={onHome} aria-label="Go home">⌂</button><div><p className="eyebrow">PICK A STORY PACK</p><h1>Let’s play!</h1></div><div/></header><div className="pack-grid">{scenePacks.map((pack)=>{const complete=pack.scenes.filter((scene)=>progress.sceneResults[scene.id]?.completed).length;return <button className="pack-card" key={pack.id} style={{'--pack-color':pack.color} as React.CSSProperties} onClick={()=>onPack(pack.id)}><span className="pack-icon">{pack.icon}</span><span><strong>{pack.name}</strong><small>{pack.description}</small><em>{complete}/{pack.scenes.length} stories</em></span><span className="pack-play">▶</span></button>})}</div></main>}
type ListProps={pack:ScenePack;progress:Progress;onBack:()=>void;onScene:(index:number)=>void};
export function SceneListScreen({pack,progress,onBack,onScene}:ListProps){return <main className="screen select-screen"><header className="screen-header"><button className="icon-button" onClick={onBack} aria-label="Back to packs">‹</button><div><p className="eyebrow">{pack.icon} {pack.name}</p><h1>Choose a story</h1></div><div/></header><div className="scene-list">{pack.scenes.map((scene,index)=><button key={scene.id} onClick={()=>onScene(index)} className="scene-row"><span className="scene-number">{index+1}</span><span><strong>{scene.title}</strong><small>Stage {scene.stage} · {scene.role}</small></span><span className={progress.sceneResults[scene.id]?.completed?'scene-done':''}>{progress.sceneResults[scene.id]?.completed?'✓':'▶'}</span></button>)}</div></main>}
