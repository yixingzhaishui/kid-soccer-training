type Props={childName:string;soundEnabled:boolean;score:number;completed:number;total:number;onChildName:(name:string)=>void;onPlay:()=>void;onToggleSound:()=>void;onParentHoldStart:()=>void;onParentHoldEnd:()=>void;holdingParent:boolean};
export function HomeScreen(props:Props){const name=props.childName.trim()||'Tom';return <main className="screen home-screen"><div className="home-stadium" aria-hidden="true"><span>⚽</span><div className="home-kid">🧒🏻</div></div><p className="eyebrow">CARTOON SOCCER STORIES</p><h1>{name}’s<br/><span>Soccer Game</span></h1><label className="child-name"><span>Player name</span><input aria-label="Player name" value={props.childName} maxLength={20} autoComplete="given-name" enterKeyHint="done" onChange={(event)=>props.onChildName(event.target.value)} onBlur={()=>props.onChildName(name)}/></label><p className="home-tagline">Watch the game. Make a choice. See what happens!</p><div className="home-score" aria-label={`${props.score} points`}>⭐ {props.score} points</div><button className="primary huge home-play" onClick={props.onPlay}>▶ Play soccer</button><p className="home-progress">{props.completed} of {props.total} stories played</p><button className="sound-button" onClick={props.onToggleSound}>{props.soundEnabled?'🔊 Sound on':'🔇 Sound off'}</button><button className={`parent-hold ${props.holdingParent?'holding':''}`} onPointerDown={props.onParentHoldStart} onPointerUp={props.onParentHoldEnd} onPointerLeave={props.onParentHoldEnd} onPointerCancel={props.onParentHoldEnd} aria-label="Parent mode. Press and hold for two seconds.">🔒 {props.holdingParent?'Keep holding…':'Parents: hold 2 seconds'}</button></main>}
