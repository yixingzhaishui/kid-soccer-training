import { describe,expect,it } from 'vitest';
import { animatedScenarios } from '../src/lessons';
import { emptyProgress,loadProgress,PROGRESS_KEY,recordResult,resetProgress,saveProgress } from '../src/logic/progress';
class MemoryStorage{values=new Map<string,string>();getItem(key:string){return this.values.get(key)??null}setItem(key:string,value:string){this.values.set(key,value)}removeItem(key:string){this.values.delete(key)}}
describe('animated story progress',()=>{it('records quality and persists the selected consequence',()=>{const storage=new MemoryStorage();const scene=animatedScenarios[0];const progress=recordResult(emptyProgress(),scene.id,scene.results[0]);saveProgress(progress,storage);expect(loadProgress(storage).sceneResults[scene.id]).toMatchObject({completed:true,plays:1,best:1,lastChoiceId:scene.results[0].choiceId})});it('loads malformed storage safely and resets',()=>{const storage=new MemoryStorage();storage.setItem(PROGRESS_KEY,'{broken');expect(loadProgress(storage)).toEqual(emptyProgress());saveProgress(recordResult(emptyProgress(),animatedScenarios[0].id,animatedScenarios[0].results[0]),storage);expect(resetProgress(storage)).toEqual(emptyProgress());expect(storage.getItem(PROGRESS_KEY)).toBeNull()})});
